<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd">

    <context:annotation-config/>

    <!-- Provide a hard-coded DataSource provider -->
    <bean id="testProvider" class="gov.nih.nlm.lode.tests.VirtuosoTestDatasourceProvider">
        <property name="serverName" value="${virtuosoServerName}"/>
        <property name="userName" value="${virtuosoUserName}"/>
        <property name="password" value="${virtuosoPassword}"/>
    </bean>

    <!-- Provide a hard-coded DataSource provider -->
    <bean id="brokenProvider" class="gov.nih.nlm.lode.tests.VirtuosoTestDatasourceProvider">
        <property name="serverName" value="${virtuosoServerName}"/>
        <property name="userName" value="notrealuser"/>
        <property name="password" value="notrealpassword"/>
    </bean>

</beans>
