^[LOOKUP.DESCRIPTORS.EXACT]
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX meshv: <http://id.nlm.nih.gov/mesh/vocab#>
PREFIX bif: <virtuoso:bif>

SELECT DISTINCT ?resource WHERE {
  ?resource a meshv:Descriptor .
  ?resource rdfs:label ?bound
}

^[LOOKUP.DESCRIPTORS.STARTSWITH]
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX meshv: <http://id.nlm.nih.gov/mesh/vocab#>
PREFIX bif: <virtuoso:bif>

SELECT DISTINCT ?resource WHERE {
  ?resource a meshv:Descriptor .
  ?resource rdfs:label ?bound
}


^[LOOKUP.DESCRIPTORS.CONTAINS]
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX meshv: <http://id.nlm.nih.gov/mesh/vocab#>
PREFIX bif: <virtuoso:bif>

SELECT DISTINCT ?resource WHERE {
  ?resource a meshv:Descriptor .
  ?resource rdfs:label ?bound
}


^[LOOKUP.ALLOWED.QUALIFIERS]
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX meshv: <http://id.nlm.nih.gov/mesh/vocab#>
PREFIX bif: <virtuoso:bif>

SELECT DISTINCT ?resource WHERE {
  ?resource a meshv:Descriptor .
  ?resource rdfs:label ?bound
}


^[LOOKUP.PAIRS.EXACT]
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX meshv: <http://id.nlm.nih.gov/mesh/vocab#>
PREFIX bif: <virtuoso:bif>

SELECT DISTINCT ?resource WHERE {
  ?resource a meshv:Descriptor .
  ?resource rdfs:label ?bound
}

^[LOOKUP.PAIRS.STARTSWITH]
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX meshv: <http://id.nlm.nih.gov/mesh/vocab#>
PREFIX bif: <virtuoso:bif>

SELECT DISTINCT ?resource WHERE {
  ?resource a meshv:Descriptor .
  ?resource rdfs:label ?bound
}

^[LOOKUP.PAIRS.CONTAINS]
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX meshv: <http://id.nlm.nih.gov/mesh/vocab#>
PREFIX bif: <virtuoso:bif>

SELECT DISTINCT ?resource WHERE {
  ?resource a meshv:Descriptor .
  ?resource rdfs:label ?bound
}
